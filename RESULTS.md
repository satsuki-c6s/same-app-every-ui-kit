# 実測値

測り方の定義は [PREREG.md](./PREREG.md)。定義版 **v2**。

## 測定環境 (第1章)

| 項目 | 値 |
|---|---|
| 測定日 | 2026-08-02 |
| Node | v25.2.1 |
| パッケージマネージャ | npm 11.6.2 |
| 土台 | React 19.2.8 / Vite 8.2.0 / TypeScript 6.0.3 (baseline のみ 7.0.2) |
| 画面 | 1920×1080 / DPR 1 |
| ブラウザ | Playwright 1.62.1 の Chromium (headless) |
| axe | axe-core 4.12.1 (既定のルールセット) |

**1回だけ測り、やり直していません。**

Mantine (第3回)・Ant Design (第4回)・daisyUI (第5回)・Chakra UI (第6回) も同じ日・同じ機械で測っています。
daisyUI のみ Tailwind CSS 4.3.3 を併用します (プラグイン方式のため必須。shadcn/ui と同じ版)。

**記載を1件訂正しました (2026-08-02)。** 「土台」の TypeScript を当初 7.0.2 と書いていましたが、
実際に 7.0.2 なのは baseline だけで、ライブラリを使う3実装 (shadcn/ui・MUI・Mantine) は 6.0.3 です。
`tsc` は型検査のみでバンドル出力を作らないため、**掲載している測定値は変わりません**。
土台を完全に揃えるという前提に対する記載漏れなので、数字ではなく記載のほうを直しました。

---

## 一覧

| | baseline (ライブラリ無し) | shadcn/ui | MUI (Community) | Mantine | Ant Design | daisyUI | Chakra UI |
|---|---|---|---|---|---|---|---|
| JS 転送量 (gzip) | 64.12 kB | 139.84 kB | 211.49 kB | 143.04 kB | **329.36 kB** | **64.36 kB** | 190.86 kB |
| CSS 転送量 (gzip) | 1.17 kB | 10.77 kB | 0 (CSS-in-JS で JS に含む) | **37.12 kB** | 0 (CSS-in-JS で JS に含む) | 10.58 kB | 0 (CSS-in-JS で JS に含む) |
| フォント | 0 | woff2 5本 (計 76.4 kB) | 0 (Roboto は読み込んでいない) | 0 | 0 | 0 | 0 |
| **自作行数** | 610 | 340 | 307 | **269** | 286 | 331 | 332 |
| **取り込み行数** | 0 | **1,472** | 0 | 0 | 0 | 0 | 0 |
| 依存パッケージ (dep + dev) | 2 + 6 | 17 + 7 | 8 + 7 | 7 + 7 | **4 + 7** | 2 + 10 | 4 + 7 |
| 共有ロジック (全実装同一) | 120 行 | 120 行 | 120 行 | 120 行 | 120 行 | 120 行 | 120 行 |

自作行数 = 各 kit の `src/` のうち自分で書いたもの。
取り込み行数 = CLI が生成して自分のリポジトリに置かれた部品のソース。

**方式の違いがここに出ています。** shadcn/ui は部品のソースを自分のリポジトリにコピーするので
取り込み 1,472 行を抱えます。MUI は npm から入れる普通のライブラリなので取り込みは 0 ですが、
そのぶん転送量が最大になります。**「書く量」「抱える量」「送る量」は別々の指標です。**

**Mantine は書く量が最少 (269 行) でした。** ただし CSS 転送量は 37.12 kB で4実装中の最大です。
Mantine 7 以降はスタイルを CSS-in-JS ではなく素の CSS で持つ設計なので、**JS ではなく CSS 側に出ます**。
JS と CSS を足すと Mantine 180.16 kB / MUI 211.49 kB / shadcn/ui 150.61 kB (別途フォント 76.4 kB) で、
**「どちらのファイルに乗るか」が違うだけの部分がある**ため、JS だけを並べて比べると判断を誤ります。

**Ant Design は 329.36 kB で5実装中の最大**でした (第4回の追記)。2位の MUI 211.49 kB に
100 kB 以上の差をつけています。ただし**部品の数がまるで違います**。次の軸7で見るとおり、
MUI で有料だった機能も、Mantine に無かった機能も、Ant Design は無料の本体に持っています。
**転送量は「積んでいる部品の量」の裏返し**でもあるので、この数字だけで良し悪しは決められません。

なお Ant Design は依存パッケージが 4 + 7 で**5実装中の最少**です。`antd` 1つに部品が
まとまっており、日付やグラフを別パッケージに分けている MUI・Mantine とは構成が違います。

**daisyUI は 64.36 kB で、ライブラリを入れていない baseline (64.12 kB) とほぼ同じ**でした
(第5回の追記)。差は 0.24 kB です。理由ははっきりしていて、**daisyUI は CSS のクラス名だけを配り、
JavaScript を1バイトも足さない**ためです。増えたぶんは CSS 側の 10.58 kB に出ています。

JS と CSS を足すと **daisyUI 74.94 kB / Ant Design 329.36 kB** で、**4.4 倍の開き**があります。
ただしこれは「軽いほうが優れている」という話ではありません。次の軸5と軸7で見るとおり、
**送っていないぶんは、動作を持っていない**ということでもあります。

---

## 軸5 アクセシビリティ

| | baseline | shadcn/ui | MUI | Mantine | Ant Design | daisyUI | Chakra UI |
|---|---|---|---|---|---|---|---|
| axe 違反 初期表示 | 2 | 2 | 2 | **3** | **3** | **3** | 2 |
| axe 違反 ダイアログ表示中 | 0 | 0 | 0 | **2** | **2** | 0 | 0 |
| axe 違反 フォーム検証エラー中 | 2 | 2 | 2 | **3** | **3** | **3** | **3** |
| 違反の内訳 | `landmark-one-main`, `region` | 同じ | 同じ | 左記 + **`color-contrast`** | 左記 + **`color-contrast`** | 左記 + **`color-contrast`** | 左記 + **エラー時のみ `color-contrast`** |
| Tab で主要5操作に到達 | 完遂 | 完遂 | 完遂 | 完遂 | 完遂 | 完遂 | 完遂 |
| **↓ と Enter でコンボボックスを選べる** | **できない** | できる | できる | できる | できる | **できない** | できる |
| **← → でタブが切り替わる** | **切り替わらない** | 切り替わる | 切り替わる | 切り替わる | 切り替わる | **切り替わらない** | 切り替わる |
| ダイアログの焦点閉じ込め | はい | はい | はい | はい | はい | はい | はい |
| Esc でダイアログが閉じる | はい | はい | はい | はい | はい | はい | はい |

**baseline・shadcn/ui・MUI の3実装は自動検査が同じ結果**でした。違反2件は内容まで同一で、
「ページに `<main>` が無い」というこちらのマークアップの問題です。**UI ライブラリを入れても直りません。**

**Mantine だけ1件多く出ました (第3回の追記)。** 増えた分は `color-contrast` で、22 箇所すべてが
Mantine の**既定の淡色トークン `dimmed`** です。白背景に `#868e96` で **3.32:1**、
WCAG AA の 4.5:1 に届きません。他の3実装で同じ用途に使った淡色 (MUI の `text.secondary` など) は
基準を満たしていました。**こちらが色を触った結果ではなく、既定値のままで出ます。**
説明文・補足行の色なので、既定のまま使うと本文以外の文字が基準を外れます。

**ダイアログ表示中に 0 にならないのも Mantine だけ**です。他の3実装は背後が走査対象から外れて 0 に
なりますが、Mantine のモーダルでは背後の要素が残るため `color-contrast` と `region` が引き続き出ます。

**なお、閉じるボタンの `aria-label` は公式ドキュメントの指示どおり設定して測っています。**
Mantine の Modal と Notification は閉じるボタンを既定で出しますが、名前は自動では付きません。
公式が `closeButtonProps` で設定するよう明記しているため、**「公式ドキュメントどおりに使う」という
事前登録の条件に従い設定した状態を正式な計測値**としています (設定前は `button-name` が追加で出ました)。

**差が出るのは動かしてみないと分からない部分だけ**です。矢印キー操作は、
ライブラリを使った2つでは効き、手で組んだ baseline では効きませんでした。
**ライブラリ同士の差はここには出ません** — shadcn/ui も MUI も同じように正しく動きます。

重要度は用途で変わります。個人開発や社内ツールなら影響は小さく、
公共・教育・医療・大企業向けでは採否に直結します。

### アクセシビリティを売りにしている実装を測りました (第6回の追記)

Chakra UI は公式サイトが「アクセシブルな React アプリを作るための部品」と掲げています。
5本かけて `color-contrast` の未達が3実装で出たので、**その主張が実測で裏付くか**を見ました。

**初期表示は2件で、6実装のうち最も少ない側**でした。`color-contrast` は出ません。
Mantine・Ant Design・daisyUI が3件だったのに対し、baseline・shadcn/ui・MUI と同水準です。
キーボード操作も全項目を満たします。**看板どおりの結果です。**

**ただしフォーム検証エラーを出すと3件に増えました。** 増えた1件は `color-contrast` で、
原因は**公式の `Field.ErrorText` の既定色**です。白背景に `#ef4444` で **3.76:1**、
WCAG AA の 4.5:1 に届きません。**こちらが指定した色ではありません。**

つまり、**平常時の画面は基準を満たすのに、エラーを出した瞬間に外れる**。
しかも外れるのは「入力を直してください」と伝える、いちばん読ませたい文字です。

| | 初期表示 | エラー表示中 |
|---|---|---|
| baseline / shadcn/ui / MUI | 2 | 2 |
| **Chakra UI** | **2** | **3** (エラー文言の色) |
| Mantine / Ant Design / daisyUI | 3 | 3 |

**6実装のうち4つで、既定の色が基準を外していました。** アクセシビリティを掲げている
実装でも、掲げていない実装でも起きています。**既定値は最後の砦ではない**ということです。

### 測り方でつまずいた点を残します (隠さず記録)

**1. こちらの実装ミスを2つ直しました。** はじめ入力欄を `Text as="label"` と自前の赤色で
組んでいたため、`label` 違反と余分な `color-contrast` が出ていました。
これは Chakra の欠陥ではなく**こちらの書き方の問題**なので、公式の `Field` 部品に
直してから測り直しています。残った `color-contrast` だけが Chakra 由来です。

**2. 部品の有無を一度誤判定しました。** 公式サイトの日付部品のページに
「Not available」と出ていたため「持っていない」と判断し、素の `<input type="date">` で
測りました。しかし**インストールしたパッケージには実装されていました**
(ドキュメントの整備が追いついていない状態)。公式部品に直して測り直しています。

この差は小さくありません。`DatePicker` を入れると **JS 転送量が 169.91 kB から
190.86 kB へ 20.95 kB 増えます**。掲載しているのは、公式部品を使った後者の値です。

**判定は公式サイトの表示だけで決めず、実際のパッケージも確認する**ことを以後の作法にします。

---

### daisyUI で「できない」が戻りました (第5回の追記)

**矢印キー操作が、第1回の baseline 以来はじめて「できない」に戻りました。**
コンボボックスは ↓ と Enter で選べず、タブは ← → で切り替わりません。
実際に押して確かめており、計測の誤判定ではありません
(タブに焦点を当てて ← → を押しても、焦点が1枚目から動きません)。

理由は単純です。**daisyUI は CSS のクラス名だけを配るライブラリで、JavaScript を持ちません。**
矢印キーで候補を動かす、選択中の項目を読み上げに伝える、といった動作はすべて
JavaScript が要ります。持っていないので、**使う側が書くことになります**。

これは欠陥ではなく設計です。転送量が baseline とほぼ同じ (64.36 kB 対 64.12 kB) なのも、
同じ理由から来ています。**送っていないぶん、動作を持っていない。**
5本並べて、この関係がはっきりしました。

| | JS 転送量 | 矢印キー操作 |
|---|---|---|
| baseline (手組み) | 64.12 kB | できない |
| **daisyUI** | **64.36 kB** | **できない** |
| shadcn/ui | 139.84 kB | できる |
| Mantine | 143.04 kB | できる |
| MUI | 211.49 kB | できる |
| Ant Design | 329.36 kB | できる |

**軽さと、正しく作るのが難しい部分は、引き換えの関係にあります。**
どちらが良いという話ではなく、**何にお金と容量を払うかという選択**です。
個人開発や社内ツールで見た目だけ整えたいなら daisyUI の軽さは効きますし、
公共・教育・大企業向けでキーボード操作が要件に入るなら、自分で書く覚悟が要ります。

`color-contrast` の違反は daisyUI でも出ました。**6実装のうち3つ**
(Mantine・Ant Design・daisyUI) で同じことが起きたことになります。

---

### 第4回で計測の欠陥を2つ見つけ、直しました (隠さず記録)

**Ant Design も違反3件で、内訳は Mantine と同じ**でした。増えた1件は `color-contrast` で、
本文以外の淡い文字が基準に届きません。**5実装のうち2つで同じことが起きた**ことになります。

この回で、**計測そのものの欠陥を2つ見つけました**。どちらも第4回の測定中に判明したものです。

**欠陥1: 別の実装を測っていた。** `npx vite preview` は実行ファイルの解決先を親ディレクトリまで
遡るため、**別の kit の vite が、別の kit のフォルダで起動する**ことがありました。
Ant Design を測ったつもりで shadcn/ui と MUI を測っていた測定が実際に発生しています。
対策として、vite の実体をパスで名指しし、さらに**配信されている画面が本当にその kit か**を
`index.html` の `<title>` で照合してから測るようにしました。違えばそこで停止します。

**欠陥2: 判定が実装依存だった。** Ant Design で最初「タブが矢印キーで動かない」「Esc で
ダイアログが閉じない」と出ましたが、**どちらも実際には正しく動いていました**。
タブは読み上げ用の文字が混ざって完全一致が外れ (`"Tab 2 of 2登録"`)、ダイアログは
DOM に残したまま非表示にする方式だったため「まだある」と誤判定していたためです。
判定を、焦点がタブに移ったか・実際に見えなくなったか、で見るように直しました。

**直したあと、5実装すべてを測り直しています。** その結果、
**公開済みの4実装 (baseline / shadcn/ui / MUI / Mantine) の値は1つも変わりませんでした**。
第1〜3回で公開した数字と主張に訂正はありません。

**ここで方針を明記します。** 計測の道具に欠陥が見つかった場合は直し、**全実装を測り直して
差分を必ず報告**します。結果を見てから条件を動かすことは行いません
(今回の修正は、Ant Design の値が出る前に判明した測定条件の欠陥への対処です)。

---

### v1 で一度測り、やり直した経緯 (隠さず記録)

v1 の共通デモは**素の HTML 要素だけで組める部品**しか含んでいませんでした。
入力欄・ボタン・表・`<dialog>` はブラウザが最初から挙動を持つため、どのライブラリでも差が出ません。
実際 v1 では axe の違反数もキーボード到達性も完全に一致しました。

そこで PREREG を v2 に上げ、コンボボックス・タブ・カレンダーを共通デモに足して測り直しました。
**まだ1本も公開していない段階での変更**で、方針は**要件を変える前に**変更履歴に記録しています。

**計測のバグも1件直しました。** 焦点の閉じ込め判定が「Tab を1回押してダイアログの内側にいるか」
という粗い実装で、押せる要素が1つしかないダイアログでは `activeElement` が `body` に見えて
誤判定していました。複数回押す判定に直した結果、全実装とも「閉じ込められている」が正しい値です。

**ダイアログ表示中だけ違反 0** になるのは、モーダル表示中に背後が `inert` 扱いになり、
初期表示にあった2件が走査対象から外れるためです。「ダイアログを出すと良くなる」ではありません。

---

## 軸2 開発速度

| | baseline | shadcn/ui | MUI | Mantine | Ant Design | daisyUI | Chakra UI |
|---|---|---|---|---|---|---|---|
| 自作行数 | 610 | 340 | 307 | **269** | 286 | 331 | 332 |
| 取り込み行数 | 0 | 1,472 | 0 | 0 | 0 | 0 | 0 |
| 実質の総量 (自作 + 取り込み) | 610 | 1,812 | 307 | **269** | 286 | 331 | 332 |

**書く量が最少なのは Mantine (269 行)** で、次が MUI (307 行) です。どちらも npm から入れて
import するだけなので、自分のリポジトリには何も増えません。shadcn/ui は書く量では近い値ですが、
取り込み 1,472 行を抱えます。

Mantine が MUI より 38 行少ないのは、**トーストとダイアログを開くのに部品を組む必要が無い**
ためです。MUI は `Snackbar` の中に `Alert` を入れ、開閉の状態も自分で持ちます。
Mantine は `notifications.show()` の1行で、状態を持ちません。
逆に**並べ替え付きの表見出しは Mantine に無い**ので、そこは自分でボタンを書いています。

**Chakra UI は 332 行で、ライブラリを使った6つのなかで最多**でした (第6回の追記)。
ただし daisyUI (331 行) との差は1行で、理由は別のところにあります。daisyUI は
「持っていない部品を自分で書いた」ぶんでしたが、**Chakra は部品を細かい部分に分けて
組む設計**なので、同じ画面でも行数が伸びます。たとえば選択欄ひとつに
`Select.Root` `Select.Control` `Select.Trigger` `Select.Positioner` … と積み上げます。
**融通は利きますが、書く量は増えます。**

**daisyUI は 331 行で、ライブラリを使った5つのなかでは最多**でした (第5回の追記)。
理由は明快で、**コンボボックスの候補リストを自分で書いている**ためです。
daisyUI が持たないので、第1回の baseline と同じ手で組みました。
**「持っていない部品のぶんだけ、書く量が増える」**という当たり前のことが数字に出ています。
それでも baseline の 610 行より 279 行少ないのは、見た目の CSS を書かずに済むからです。

**Ant Design は 286 行で、Mantine と MUI のあいだ**でした。書く量では大きな差が出ません。
ただし1つだけ注意があります。Ant Design の `Typography.Paragraph` は `<p>` を描画せず
`<div>` になるため、他実装と同じ土俵に乗せるにはここだけ素の `<p>` で囲んでいます
(見た目は変えていません)。**同じ画面を作っても、出てくる HTML は実装ごとに違います。**

v1 (6要件) から v2 (9要件) で shadcn の取り込み行数が 708 → 1,472 行に増えたのは、
コンボボックスとカレンダーが重いためです (`calendar.tsx` だけで 8.1 kB)。
**部品を1つ足すごとに、抱えるコードが目に見えて増えます。**

### 手数 (スキャフォールドから完成まで)

shadcn/ui は6手。

1. `npm create vite@latest shadcn-ui -- --template react-ts`
2. `npm i tailwindcss @tailwindcss/vite`
3. `src/index.css` を `@import "tailwindcss";` の1行に
4. `@` エイリアスを `vite.config.ts` と `tsconfig` に追加
5. `npx shadcn@latest init -y -b radix -p nova`
6. `npx shadcn@latest add button input label table dialog sonner tabs popover command calendar`

MUI は2手。

1. `npm create vite@latest mui -- --template react-ts`
2. `npm i @mui/material @emotion/react @emotion/styled @mui/icons-material @mui/x-date-pickers dayjs`

Mantine も2手。

1. `npm create vite@latest mantine -- --template react-ts`
2. `npm i @mantine/core @mantine/hooks @mantine/dates @mantine/notifications dayjs`

**実際にはスキャフォールドせず、第2回 (MUI) の設定ファイルを複製しました。** 土台の版を
完全に一致させるためです (React・Vite・TypeScript・プラグインの版を揃える)。上の2手は
公式手順どおりに始めた場合の手数で、**やっていない手順を実行したことにはしていません**。

**公式の Vite 手順にある `postcss-preset-mantine` は入れていません。** これは自分で CSS を書いて
Mantine のミックスインを使う場合に要るもので、今回は独自の CSS を1行も書いていないため
不要でした。入れなくてもビルドは通り、共通デモは9要件すべて動きます。

**shadcn の CLI は書式が変わっています** (4.16.1 時点)。`-b` は色ではなく**部品ライブラリの選択**
(`base` / `radix` / `aria`)、配色テーマは `-p`。色名を `-b` に渡す古い手順は通りません。

**MUI 7 以降はシステムプロパティが外れています。** `<Stack justifyContent>` や
`<Typography fontWeight>` は型エラーになり、`sx` に移す必要があります。
古い記事のコードはそのままではビルドが通りません。

---

## 軸3 自由度 (改造3課題)

改造は別途行い、**測定対象の実装には残していません** (改造後に既定値へ戻し、撮り直しています)。

| 課題 | baseline | shadcn/ui | MUI | Mantine | Ant Design | daisyUI | Chakra UI |
|---|---|---|---|---|---|---|---|
| (a) ブランド色1つで全体を差し替え | 可能 / 2箇所 | 可能 / 2箇所 (`--primary`) | 可能 / **1箇所** (`createTheme`) | 可能 / 1箇所 (`createTheme`)。ただし色は10段階を渡す | 可能 / **1箇所・1値** (`token.colorPrimary`) | 可能 / **1箇所・1値** (`--color-primary`) | 可能 / 1箇所。ただし**11段階 + 7項目** |
| (b) 全部品の角丸を 0 に | 該当なし (既定で角丸なし) | 可能 / 1箇所 (`--radius`) | 可能 / **(a) と同じ1箇所** (`shape.borderRadius`) | 可能 / (a) と同じ1箇所 (`defaultRadius`) | 可能 / **(a) と同じ1箇所** (`token.borderRadius`) | 可能 / (a) と同じ1箇所。ただし**3値**を指定 | 可能 / (a) と同じ1箇所。**3値**を指定 |
| (c) ダイアログの開閉アニメを差し替え | 新規に書く必要あり | 可能 / 2箇所 (`dialog.tsx` を直接編集) | 可能 (`TransitionComponent` が公式の口) | 可能 / (a) と同じ1箇所 (`components.Modal.defaultProps`) | 可能 / **(a) と同じ1箇所** (`components.Modal`) | **新規に書く必要あり** (公式の口が無い) | 可能 (`motionPreset` が公式の口) |

Chakra UI (第6回) は3課題とも可能で、場所は `createSystem` の1箇所です
((c) は `Dialog` の `motionPreset` が公式の口)。
ただし**渡す値の量が6実装で最も多くなりました**。色は 50〜950 の**11段階**に加えて、
`semanticTokens` で `solid` `contrast` `fg` `muted` `subtle` `emphasized` `focusRing` の
**7項目**を書きます。Mantine の10段階より重く、Ant Design や daisyUI の「1値」とは大きな差です。
角丸も `l1` `l2` `l3` の3値を指定します。**「1箇所」と「1値」は別だ**という第3回の指摘が、
ここでいちばん極端に出ました。

**MUI は (a) と (b) が同じ `createTheme` の呼び出し1箇所で済みました。** テーマが1つの入口に
まとまっている設計です。shadcn/ui は CSS 変数を書き換える方式で、明暗2つぶん触ります。

(c) の差し替え方が違います。shadcn/ui は**部品のソースが手元にある**ので `dialog.tsx` を直接開いて
書き換えます。MUI は**公式が用意した差し替え口** (`TransitionComponent`) を使います。
前者は何でもできる代わりに本家の修正が自動で来ません。後者は用意された範囲でだけ変えられます。

(b) が baseline で「該当なし」なのは、素の HTML と最小限の CSS ではそもそも角丸を付けていないためです。

**Mantine は3課題すべてが `createTheme` の呼び出し1つに収まりました。** 配色・角丸に加えて
**ダイアログのアニメまで同じテーマ定義の中で指定できる**のはこの実装だけです
(`components.Modal.defaultProps` に既定の props を置く仕組みがあるため)。
確認は実行時の CSS 変数で行い、`--mantine-radius-default` が `0rem` に、
`--mantine-primary-color-filled` が指定色に変わることを見ています。

(a) には注意点があります。**Mantine の色は10段階の配列で渡す規約**なので、「ブランド色1つ」を
そのまま渡せません。1色から10段階を作る手間が要ります (公式にも生成の案内があります)。
**「1箇所で済む」ことと「1値で済む」ことは別**です。

**Ant Design は1箇所かつ1値でした。** `token.colorPrimary` に色を1つ渡すだけで、
必要な濃淡は自動で作られます。3課題すべてが `ConfigProvider` の `theme` に収まる点は
Mantine と同じで、**渡す手間まで含めると Ant Design がいちばん軽い**という結果です。
確認は実行時の計算済みスタイルで行い、主ボタンの背景色が指定色 (`rgb(95, 90, 175)`) に、
角丸が `0px` に変わることを見ています。検証後は既定へ戻し、ビルド出力のハッシュ・
gzip 値・行数が一致することを確かめました。

**daisyUI は (a)(b) が CSS の1箇所で済み、色も1値で渡せます** (第5回の追記)。
テーマを CSS 変数で定義する方式なので、`--color-primary` に色を書くだけです。
ただし角丸は `--radius-field` `--radius-box` `--radius-selector` の**3値**を指定します
(入力欄・箱・選択肢で別々に持っているため)。

**(c) は baseline と同じ「新規に書く必要あり」**でした。公式ドキュメントには
位置 (`modal-bottom`) や開き方の選択肢はありますが、**開閉アニメを差し替える公式の口が
示されていません**。CSS を自分で書くことになります。
JavaScript を持たないライブラリなので、動きに関わる部分はここでも自前です。

Mantine の改造検証は `main.tsx` 上で行い、検証後に元へ戻してビルドし直しています。
ビルド出力のハッシュ・gzip 値・行数が改造前と一致することを確認済みで、
**改造版でキャプチャは撮っていません** (第1回で起きた取り違えの再発防止)。

### 改造の確認で1回やらかしています (記録)

改造したビルドのまま `capture.mts` を回してしまい、shadcn/ui の正式なキャプチャ10枚を
改造版で上書きしました。既定値へ戻して撮り直し済みです。

---

## 軸6 保守性 (転記・2026-08-02 取得)

出典: GitHub API と npm registry API。

| 項目 | shadcn/ui (`shadcn-ui/ui`) | MUI (`mui/material-ui`) | Mantine (`mantinedev/mantine`) | Ant Design (`ant-design/ant-design`) | daisyUI (`saadeghi/daisyui`) | Chakra UI (`chakra-ui/chakra-ui`) |
|---|---|---|---|---|---|---|
| スター数 | 120,287 | 98,659 | 31,520 | 98,897 | 41,913 | 40,549 |
| 未解決 issue | 2,199 | 1,489 | 53 | 1,112 | 30 | **20** |
| 最終更新 | 2026-07-31 | 2026-07-31 | 2026-07-31 | 2026-08-02 | 2026-08-02 | 2026-07-30 |
| 最新リリース | `shadcn@4.16.1` (2026-07-31) | `v9.2.0` (2026-07-03) | `9.5.0` (2026-07-27) | `6.5.3` (2026-07-31) | `v5.7.11` (2026-08-02) | `3.36.1` (2026-07-19) |
| 直近90日のコミット数 | 234 | 291 | 293 | **616** | 218 | **81** |
| 週間ダウンロード数 | 7,335,793 (`shadcn` CLI) | 10,187,092 (`@mui/material`) | 2,301,982 (`@mantine/core`) | 3,686,447 (`antd`) | 845,641 (`daisyui`) | 1,812,128 (`@chakra-ui/react`) |
| ライセンス | MIT | MIT (Core) | MIT (全パッケージ) | MIT | MIT | MIT |

**ダウンロード数を単純に並べられません。** shadcn/ui の数字は**CLI パッケージ**のもので、
利用プロジェクト数ではありません (部品は npm から入れない方式のため)。
MUI の数字は部品パッケージそのものです。**性質が違う数字を並べるときは必ずこの但し書きを付けます。**

参考: `@mui/x-date-pickers` 5,186,764 / `@mui/x-data-grid` 2,990,390 (週間・同日取得)

**Mantine は規模が一回り小さく、開発の勢いは同等**です。スターも週間ダウンロード数も
3社中で最少ですが、直近90日のコミット数は 293 で最多、最終更新も同じ日です。
**未解決 issue 53 件は他の2つより一桁少ない値**ですが、これは「品質が高い」とも
「利用者が少ないので報告が集まらない」とも読めます。**この数字だけで良し悪しは決められません。**
週間ダウンロード数の 2,301,982 は `@mantine/core` のもので、
shadcn/ui の数字 (CLI パッケージ) とは性質が違います。MUI の `@mui/material` とは同じ性質で比べられます。

**Chakra UI は直近90日のコミット数 81 で、6つのなかで最少**でした (第6回の追記)。
最多だった Ant Design の 616 と比べると 8分の1 以下です。未解決 issue も 20 件で最少ですが、
**「issue が少ない」と「開発が活発でない」は同時に成り立ちます**。どちらの読み方も可能なので、
この2つの数字だけで健全性は判断できません。週間ダウンロード 181万は6つの中位です。

**daisyUI は未解決 issue 30 件で5つのなかで最少**、リリースは測定当日 (`v5.7.11`) でした
(第5回の追記)。週間ダウンロード 845,641 は最少ですが、**daisyUI は npm を通さず
CDN で使うこともできる**ため、この数字は利用実態の下限だと考えたほうが正確です。
性質の違う数字を並べるときの但し書きは、shadcn/ui の CLI と同じ扱いになります。

**Ant Design は直近90日のコミット数 616 で、他の2倍前後**でした (第4回の追記)。
最終更新も測定当日です。スター数は MUI とほぼ並び、週間ダウンロードは 368万で
MUI に次ぐ2位。**開発の勢いという点では4つの中で最も活発**です。
未解決 issue 1,112 件は shadcn/ui より少なく MUI と同水準ですが、
**issue の数は利用者数と運用方針の両方で動く**ので、単独では品質の指標になりません。

---

## 軸7 必要な部品 (15項目・2026-08-02 取得)

出典: shadcn は registry index (`https://ui.shadcn.com/r/index.json`、62件)、
MUI は npm registry の `license` フィールドと公式の価格ページ、
Mantine は公式ドキュメントの各部品ページと npm registry の `license` フィールド。

| # | 部品 | baseline | shadcn/ui | MUI | Mantine | Ant Design | daisyUI | Chakra UI |
|---|---|---|---|---|---|---|---|---|
| 1 | テーブル | 無し (自作) | 無料 | 無料 | 無料 | 無料 | 無料 (見た目のみ) | 無料 (並べ替えは自作) |
| 2 | データグリッド (仮想化・列固定) | 無し | **無し** | **有料** (基本は無料・列固定などが Pro) | **無し** | **無料** (`virtual` と `fixed`) | **無し** | **無し** |
| 3 | 日付入力 | 無し (ブラウザ標準) | 無料 | 無料 (`x-date-pickers` は MIT) | 無料 (`@mantine/dates`) | 無料 (本体に同梱) | 無料 (ブラウザ標準の装飾) | 無料 (DatePicker) |
| 4 | カレンダー | 無し (ブラウザ標準) | 無料 | 無料 | 無料 | 無料 | **無し** (別ライブラリに style を当てる案内のみ) | 無料 (DatePicker の盤面) |
| 5 | グラフ | 無し | 無料 | 無料 (`x-charts` は MIT) | 無料 (`@mantine/charts`) | 無料 (`@ant-design/charts`) | **無し** (ApexCharts の作例集) | **無し** |
| 6 | コンボボックス | 無し (自作) | 無料 | 無料 (Autocomplete) | 無料 (Combobox / Select) | 無料 (Select) | **無し** (自作した) | 無料 (Combobox) |
| 7 | コマンドパレット | 無し | 無料 | **無し** | 無料 (`@mantine/spotlight`) | **無し** | **無し** | **無し** |
| 8 | トースト | 無し (自作) | 無料 | 無料 (Snackbar) | 無料 (`@mantine/notifications`) | 無料 (message) | 無料 (見た目のみ) | 無料 (toaster) |
| 9 | ダイアログ | 無し (`<dialog>`) | 無料 | 無料 | 無料 (Modal) | 無料 (Modal) | 無料 (`<dialog>` の装飾) | 無料 (Dialog) |
| 10 | ドロワー | 無し | 無料 | 無料 | 無料 | 無料 | 無料 (CSS のみ) | 無料 |
| 11 | タブ | 無し (自作) | 無料 | 無料 | 無料 | 無料 | 無料 (見た目のみ) | 無料 |
| 12 | フォーム検証 | 無し (自作) | 無料 | **無し** (表示は可・検証機構は別ライブラリ) | 無料 (`@mantine/form`) | 無料 (Form の rules) | 無料 (HTML 標準の結果を着色) | 無料 (Field) |
| 13 | ファイルアップロード | 無し | 判定保留 | 無し | 無料 (`@mantine/dropzone`) | 無料 (Upload) | 無料 (`file-input` の装飾) | 無料 (FileUpload) |
| 14 | リッチテキスト | 無し | **無し** | 無し | 無料 (`@mantine/tiptap`) | **無し** | **無し** | **無し** |
| 15 | ページネーション | 無し | 無料 | 無料 | 無料 | 無料 | 無料 (見た目のみ) | 無料 |

### Chakra UI は15項目中11が無料でした (第6回の追記)

無いのは4つ、**データグリッド・グラフ・コマンドパレット・リッチテキスト**です。
有料はありません (MIT)。日付入力とカレンダーは公式の `DatePicker` が持っています。

**判定を一度間違えました。** 公式サイトの日付部品のページに「Not available」と出ていたので
「持っていない」と判断しましたが、**インストールしたパッケージには実装されていました**。
ドキュメントの整備が追いついていない状態です。公式部品に直して測り直しています
(詳しくは軸5の記録)。**公式サイトの表示だけで判定しない**ことを以後の作法にします。

---

### daisyUI の「無料」は意味が違います (第5回の追記)

daisyUI は **CSS のクラス名だけを配るライブラリ**なので、上の表の「無料」は
**見た目のクラスがある**という意味であって、**動作を持っているとは限りません**。

| 種類 | daisyUI での扱い | 例 |
|---|---|---|
| 見た目だけ | クラスはあるが動作は自分で書く | テーブル (並べ替えは自作)、トースト (出し入れは自作)、ページネーション (ページ送りは自作) |
| ブラウザの機能に乗る | 素の HTML の動作をそのまま使う | ダイアログ (`<dialog>` の焦点閉じ込めと Esc)、フォーム検証、日付入力 |
| 別ライブラリ前提 | **無し**と数えた | カレンダー (Cally 等)、グラフ (ApexCharts) |
| 本当に無い | 代替の案内も無い | データグリッド、コンボボックス、コマンドパレット、リッチテキスト |

**「別ライブラリにスタイルを当てる方法がある」ことは、持っていることとは違います。**
公式が案内していても、導入するのは別のライブラリだからです。
この基準で数えると、15項目中 **無料10 / 有料0 / 無し5** になります。

**カレンダーが「無し」になった影響が、キャプチャにそのまま出ました。**
`08-calendar.png` を見ると、**盤面がどこにも写っていません**。素の `<input type="date">` を
使ったので、盤面はブラウザ (と OS) が描いており、ページの中に存在しないためです。
これは第1回の baseline とまったく同じ現象で、**他の4実装では全部写っていました**。

### データグリッドが3回ぶんの答え合わせになりました (第4回の追記)

**同じ部品を、3つのライブラリが3通りに扱っています。**

| | データグリッド (仮想化・列固定) |
|---|---|
| MUI (第2回) | **有料**。Pro は1年1開発者 299 ドル |
| Mantine (第3回) | **無し**。公式に持たない (第三者製はある) |
| **Ant Design (第4回)** | **無料**。`Table` に `virtual` を渡すと仮想スクロール、列は `fixed` で固定 |

第2回では「業務用の分厚い表を作り始めるとお金が要る」と言い、第3回では「全部無料の代わりに
ここが欠けている」と言いました。**第4回はその両方を無料で持っています。**
つまり**「データグリッドは高機能だからお金が要る」わけではない**ということです。
どこを無料にしてどこで採算を取るかは、**そのライブラリの方針の問題**でした。

Ant Design は15項目中 **無料13 / 有料0 / 無し2**。無いのはコマンドパレットとリッチテキストです。
エコシステム (`@ant-design/charts` `pro-components` `pro-table` `x`) も確認した範囲ではすべて MIT で、
**有料版という区分がありません**。

**Mantine は15項目中14が無料、有料は0、無しは1 (データグリッド) です。**
第3回で調べた10パッケージ (`core` `hooks` `dates` `notifications` `form` `charts` `spotlight`
`tiptap` `dropzone` `carousel`) は**すべて 9.5.0 / MIT** で、有料版という区分がありません。

**MUI に無かった3つ (コマンドパレット・フォーム検証・ファイルアップロード) を Mantine は無料で持ちます。**
逆に**データグリッドは公式に無い**のが唯一の欠けです。`Table` に固定見出しはありますが、
仮想化や列固定は持ちません (よく名前の挙がる `mantine-datatable` は公式ではなく第三者製です)。
**大量データの表が要件の中心なら、ここは Mantine 単体では埋まりません。**

### 無料と有料の境界 (ここが第2回の本題)

**shadcn/ui 本体に有料版はありません。** 62部品すべて MIT です。
有料が出てくるのは第三者が売っている画面テンプレート集で、本体の機能制限ではありません。

**MUI は本体 (Core) が MIT で、有料なのは MUI X の一部の機能です。** 価格は公式ページより:

| プラン | 価格 |
|---|---|
| Community | 無料 |
| Pro | **$299 / 年 / 開発者** |
| Premium | $599 / 年 / 開発者 |
| Enterprise | $1,399 / 年 / 開発者 |

**「グラフや日付入力そのものが有料」ではありません。** 各系統に MIT の無料版があります
(npm の `license` を直接確認済み):

| 無料 (MIT) | 有料 (商用ライセンス) |
|---|---|
| `@mui/x-data-grid` | `@mui/x-data-grid-pro` |
| `@mui/x-date-pickers` | `@mui/x-date-pickers-pro` |
| `@mui/x-charts` | `@mui/x-charts-pro` |
| `@mui/x-tree-view` / `@mui/x-scheduler` | — |

Pro で増えるのは**表の高度な機能**が中心です: 列の固定・列のリサイズ・列のグループ化・
行の並べ替え・行の固定・セル選択・ヘッダフィルタ・行/セルの編集・Excel 書き出し・
ツリーデータ・行グループ化・集計。Premium はさらにピボットや大規模データ向けが加わります。

**今回の共通デモ9要件は、MUI でも全部無料の範囲で作れました。** 有料が要るのは
「業務用の分厚い表」を作り始めてからです。

---

## キャプチャ

`baseline/shots/` `shadcn-ui/shots/` `mui/shots/` `mantine/shots/` `antd/shots/`
`daisyui/shots/` `chakra/shots/` に10枚ずつ。
**同じスクリプト (`scripts/capture.mts`) が同じ順で操作したもの**で、番号が同じなら同じ場面です。

`01-initial` `02-search` `03-combobox` `04-filter` `05-sorted` `06-dialog`
`07-tab-form` `08-calendar` `09-form-error` `10-dark`

**カレンダーの開き方が実装ごとに違いました** (撮影スクリプトを実装非依存に直しています):

- baseline: `<input type="date">`。盤面はブラウザ (と OS) のもので、**画面にもキャプチャにも写りません**
- shadcn/ui: `id="date"` のボタンを押すと盤面がページ内に開く
- MUI: 入力欄が分割入力なので**欄を押しても開かず**、右端のカレンダーアイコンでのみ開く
- Mantine: 入力欄そのものがボタン (`DatePickerInput`)。押すと盤面がページ内に開く
- Ant Design: 入力欄を押すと盤面が開く。**盤面は既定で英語表記** (`Aug 2026`) で、
  週の始まりが**日曜** (Mantine は月曜始まり)。日本語化はできますが、既定値のまま撮っています。
  4実装で唯一、**年送りの二重矢印**と**「Today」ボタン**が既定で付きます
- daisyUI: **baseline と同じく盤面が写りません**。カレンダーを持たないので素の
  `<input type="date">` を使っており、盤面はブラウザ (と OS) 側が描くためです

**Mantine の盤面は既定で英語表記**です (`Mo Tu We` / `August 2026`)。画面の他の文言は日本語なので
ここだけ英語になります。日本語化はできますが**既定値のみルールに従い、既定のまま撮っています**。

---

## まだ測っていないもの

- 公式ドキュメントに検索があるか (軸6) — **Mantine と Ant Design は確認済み**。
  shadcn/ui と MUI は未確認のまま
- MUI の `attachment` 相当 (ファイルアップロード) の有無確認
- shadcn/ui のファイルアップロード (第1回で「判定保留」のまま)
