# 実測値

測り方の定義は [PREREG.md](./PREREG.md)。定義版 **v2**。

## 測定環境 (第1章)

| 項目 | 値 |
|---|---|
| 測定日 | 2026-08-02 |
| Node | v25.2.1 |
| パッケージマネージャ | npm 11.6.2 |
| 土台 | React 19.2.8 / Vite 8.2.0 / TypeScript 7.0.2 |
| 画面 | 1920×1080 / DPR 1 |
| ブラウザ | Playwright 1.62.1 の Chromium (headless) |
| axe | axe-core 4.12.1 (既定のルールセット) |

**1回だけ測り、やり直していません。**

---

## 一覧

| | baseline (ライブラリ無し) | shadcn/ui |
|---|---|---|
| JS 転送量 (gzip) | 64.12 kB | 139.84 kB |
| CSS 転送量 (gzip) | 1.17 kB | 10.77 kB |
| フォント | 0 | woff2 5本 (計 76.4 kB) |
| **自作行数** | 610 | **340** |
| **取り込み行数** | 0 | **1,472** |
| 依存パッケージ (dep + dev) | 2 + 6 | 17 + 7 |
| 共有ロジック (両者同一) | 120 行 | 120 行 |

自作行数 = 各 kit の `src/` のうち自分で書いたもの。
取り込み行数 = shadcn CLI が生成した `src/components/ui/*` `src/lib/*` `src/index.css`。

---

## 軸5 アクセシビリティ — ここが一番はっきり分かれた

| | baseline | shadcn/ui |
|---|---|---|
| axe 違反 初期表示 | 2 | 2 |
| axe 違反 ダイアログ表示中 | 0 | 0 |
| axe 違反 フォーム検証エラー中 | 2 | 2 |
| 違反の内訳 | `landmark-one-main`, `region` | **同じ** |
| Tab で主要5操作に到達 | 完遂 | 完遂 |
| **コンボボックスを ↓ と Enter で選べる** | **できない** | **できる** |
| **タブが ← → で切り替わる** | **切り替わらない** | **切り替わる** |
| ダイアログの焦点閉じ込め | はい | はい |
| Esc でダイアログが閉じる | はい | はい |

**自動検査では差が出ません。** 違反2件は両実装で同一で、内容は「ページに `<main>` が無い」。
これはこちらのマークアップの問題で、**UI ライブラリを入れても直りません**。

**差は「動かしてみないと分からない部分」に出ました。**
コンボボックスとタブの矢印キー操作は、仕様どおりに実装しないと動きません。
手で組んだ baseline では効かず、shadcn/ui では効きます。
これが「UI ライブラリを使う実利」の実測値です。見た目は CSS を頑張れば真似できますが、
この挙動は真似できません。

**重要度は用途で変わります。** 個人開発や社内ツールなら影響は小さい。
公共・教育・医療・大企業向けでは、この差がそのまま採否に効きます。

### v1 で一度測り、やり直した経緯 (隠さず記録)

v1 の共通デモは**素の HTML 要素だけで組める部品**しか含んでいませんでした。
入力欄・ボタン・表・`<dialog>` はブラウザが最初から挙動を持つため、
**どのライブラリで作っても差が出ません**。実際 v1 では axe の違反数もキーボード到達性も
両実装で完全に一致しました。

そこで PREREG を v2 に上げ、コンボボックス・タブ・カレンダーを共通デモに足して測り直しました。
**まだ1本も公開していない段階での変更**であり、方針は**要件を変える前に**
PREREG.md の変更履歴に記録しています。公開済みの回が出た後は同じことをしません。

もう一つ、**計測のバグも1件直しました**。焦点の閉じ込め判定が「Tab を1回押して
ダイアログの内側にいるか」という粗い実装で、押せる要素が1つしかないダイアログでは
`activeElement` が `body` に見えて誤判定していました。複数回押す判定に直した結果、
両実装とも「閉じ込められている」が正しい値です。

**ダイアログ表示中だけ違反 0** になるのは、モーダル表示中に背後が `inert` 扱いになり、
初期表示にあった2件が走査対象から外れるためです。「ダイアログを出すと良くなる」ではありません。

---

## 軸2 開発速度

**同じ課題を、同じロジックで、UI だけ書き換えた結果:**

- 自作行数だけを見ると shadcn/ui は **610 → 340 行 (約半分)**
- 取り込み 1,472 行を足すと **1,812 行**で、baseline より大幅に多い

**どちらの数字も嘘ではありません。** shadcn/ui はコンポーネントのソースを自分のリポジトリへ
コピーして持つ方式なので、「書いた量」と「抱える量」が一致しません。
PREREG.md で決めたとおり、**主の数字は自作行数、取り込み行数を必ず併記**します。

v1 (6要件) から v2 (9要件) で取り込み行数が 708 → 1,472 行に増えたのは、
コンボボックスとカレンダーが重いためです (`calendar.tsx` だけで 8.1 kB)。
**部品を1つ足すごとに、抱えるコードが目に見えて増えます。**

### 手数 (スキャフォールドから完成まで)

shadcn/ui: 6手。

1. `npm create vite@latest shadcn-ui -- --template react-ts`
2. `npm i tailwindcss @tailwindcss/vite`
3. `src/index.css` を `@import "tailwindcss";` の1行に
4. `@` エイリアスを `vite.config.ts` と `tsconfig` に追加
5. `npx shadcn@latest init -y -b radix -p nova`
6. `npx shadcn@latest add button input label table dialog sonner tabs popover command calendar`

**CLI が変わっています** (shadcn 4.16.1 時点):
`-b` は色ではなく**部品ライブラリの選択** (`base` / `radix` / `aria`)、
配色テーマは `-p` (`nova` / `vega` / `maia` / `lyra` / `mira` / `luma` / `sera` / `rhea`)。
色名を `-b` に渡す古い手順は通りません。

---

## 軸1 見た目の差 (採点しない)

同じ番号のキャプチャを並べた観察。

| 箇所 | baseline | shadcn/ui |
|---|---|---|
| 書体 | `system-ui` | Geist (init が `@fontsource-variable/geist` を入れる) |
| 行の高さ | 詰まっている | 広い |
| タブ | 下線だけの素朴な形 | 角丸の箱に収まったピル型 |
| コンボボックス | 入力欄の下に候補が並ぶだけ | 検索欄つきのカード。影と角丸 |
| **カレンダー** | **ブラウザ標準。画面に描かれない (OS の部品)** | **盤面がページ内に描かれる** |
| 入力欄 | 1px の直線ボーダー | 角丸 + フォーカスリング |
| リンク | 青 + 下線 | 本文色のまま、下線なし |
| テーブル | 罫線がはっきり | 罫線が薄い |
| トースト | 画面右下の枠付きテキスト | sonner。アイコン付きのカード |

**カレンダーの差が最も大きい。** baseline は `<input type="date">` に頼るため、
盤面はブラウザ (と OS) のもので、**スクリーンショットにすら写りません**
(`baseline/shots/08-calendar.png` は入力欄しか写っていない — これは撮影の失敗ではなく、
素の実装ではそうなる、という事実です)。見た目を揃えたいなら自作するしかありません。

**注意**: 書体の違いは「部品の設計」ではなく **init が自動でフォントを入れたこと**による差です。
見た目の印象の一部はここから来ています。混同しないよう動画内で明言します。

---

## キャプチャ

`baseline/shots/` と `shadcn-ui/shots/` に10枚ずつ。
**同じスクリプト (`scripts/capture.mts`) が同じ順で操作したもの**で、番号が同じなら同じ場面です。

`01-initial` `02-search` `03-combobox` `04-filter` `05-sorted` `06-dialog`
`07-tab-form` `08-calendar` `09-form-error` `10-dark`

操作は積み上げ式です (並べ替えた状態のままダイアログを開き、そのままタブを移る)。
全 kit で同じ順に操作しているため、比較には影響しません。

---

## 軸6 保守性 (転記・取得日 2026-08-02)

出典: GitHub API (`shadcn-ui/ui`) と npm registry API。

| 項目 | shadcn/ui |
|---|---|
| スター数 | 120,287 |
| 未解決 issue | 2,199 |
| 最終更新 | 2026-07-31 |
| 最新リリース | `shadcn@4.16.1` (2026-07-31) |
| 直近90日のコミット数 | 234 |
| 週間ダウンロード数 | 7,335,793 (CLI パッケージ `shadcn`) |
| ライセンス | MIT |
| 公式ドキュメントの検索 | **未確認** (ページがクライアント描画のため HTML 取得では判定できなかった) |

**未解決 issue が 2,199 件**あるのは、放置ではなく利用者数の規模を反映した数字と読むべきです
(週700万ダウンロード)。ただし「多い」という事実は事実として出します。

**ダウンロード数の注意**: この数字は**CLI パッケージ**のもので、
「shadcn/ui を使っているプロジェクト数」ではありません。
shadcn/ui はコンポーネントを npm から入れる方式ではないため、
他のライブラリの週間ダウンロード数と**単純に並べられません**。
比較表に載せるときは必ずこの但し書きを付けます。

---

## 軸7 必要な部品 (15項目・取得日 2026-08-02)

出典: shadcn の registry index (`https://ui.shadcn.com/r/index.json`、**62 件**)。

| # | 部品 | baseline | shadcn/ui | 備考 |
|---|---|---|---|---|
| 1 | テーブル | 無し (自作) | 無料 | `table` |
| 2 | データグリッド (仮想化・列固定) | 無し | **無し** | registry に `data-table` は無い。TanStack Table と組み合わせる案内 |
| 3 | 日付入力 | 無し (ブラウザ標準) | 無料 | `popover` + `calendar` を自分で組む。単体の `date-picker` は無い |
| 4 | カレンダー | 無し (ブラウザ標準) | 無料 | `calendar` |
| 5 | グラフ | 無し | 無料 | `chart` |
| 6 | コンボボックス | 無し (自作) | 無料 | `combobox` |
| 7 | コマンドパレット | 無し | 無料 | `command` |
| 8 | トースト | 無し (自作) | 無料 | `sonner` / `toast` |
| 9 | ダイアログ | 無し (`<dialog>`) | 無料 | `dialog` / `alert-dialog` |
| 10 | ドロワー | 無し | 無料 | `drawer` / `sheet` |
| 11 | タブ | 無し (自作) | 無料 | `tabs` |
| 12 | フォーム検証 | 無し (自作) | 無料 | `form` |
| 13 | ファイルアップロード | 無し | **判定保留** | `attachment` があるが用途を確認できていない (docs が 307 で移動) |
| 14 | リッチテキスト | 無し | **無し** | `textarea` のみ |
| 15 | ページネーション | 無し | 無料 | `pagination` |

### shadcn/ui に「有料版」はありません

**このシリーズで扱う「無料/有料」の軸は、shadcn/ui では一度空振りします。**
本体は MIT で全部無料です。有料が出てくるのは**別売りのブロック集**
(shadcnblocks、Tailwind Plus など、第三者が売っている画面テンプレート) であって、
shadcn/ui 本体の機能制限ではありません。ここを混同しないよう動画で明言します。

有料/無料の境界がはっきり出るのは MUI (Community と X Pro) の回です。
第1回では「無料でここまで全部できる」ことがそのまま結論になります。

---

## 軸3 自由度 (改造3課題・2026-08-02 測定)

改造は別途行い、**測定対象の実装には残していません** (改造後に既定値へ戻し、撮り直しています)。

| 課題 | baseline | shadcn/ui |
|---|---|---|
| (a) ブランド色1つで全体の配色を差し替え | 可能 / **2箇所** (`--accent` の明暗2つ) | 可能 / **2箇所** (`--primary` の明暗2つ) |
| (b) 全部品の角丸を 0 にする | **該当なし** (既定で角丸を使っていない) | 可能 / **1箇所** (`--radius: 0rem`) |
| (c) ダイアログの開閉アニメを差し替え | **差し替えではなく新規に書く必要あり** (アニメ指定が0件。`@starting-style` から自分で書く) | 可能 / **2箇所** (`dialog.tsx` の `duration-100`) |

**どちらも「色は1箇所で全部変わる」**という点は同じでした。CSS 変数を使う設計が共通だからです。

差が出たのは (c) です。shadcn/ui は**コンポーネントのソースが自分のリポジトリにある**ので、
`dialog.tsx` を直接開いて書き換えられます。これは「取り込み方式」の利点がそのまま出た形で、
取り込み行数 1,472 行という数字の裏返しでもあります。**抱えるコードが多い代わりに、
どこでも書き換えられる。**

(b) が「該当なし」なのは baseline が地味だからではなく、
**素の HTML と最小限の CSS では、そもそも角丸を付けていない**ためです。
「角丸を消す」課題が成立しません。これは baseline の性質を示す結果として記録します。

### 改造の確認で1回やらかしています (記録)

改造したビルドのまま `capture.mts` を回してしまい、**正式なキャプチャ10枚を
改造版で上書き**しました。既定値へ戻して撮り直し済みです。
`shadcn-ui/shots/` にあるのは既定値のみの画面です。
今後は改造を別ディレクトリで行います (PREREG.md §2 の指示どおりに運用する)。

---

## まだ測っていないもの

- 公式ドキュメントに検索があるか (軸6・未確認のまま)
