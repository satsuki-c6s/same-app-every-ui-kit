# 実測値

測り方の定義は [PREREG.md](./PREREG.md)。定義版 **v2**。

## 測定環境 (第1章)

| 項目 | 値 |
|---|---|
| 測定日 | 2026-08-02 |
| Node | v25.2.1 |
| パッケージマネージャ | npm 11.6.2 |
| 土台 | React 19.2.8 / Vite 8.2.0 / TypeScript 6.0.3 (baseline のみ 7.0.2) |
| 画面 | 1920×1080 / DPR 1 |
| ブラウザ | Playwright 1.62.1 の Chromium (headless) |
| axe | axe-core 4.12.1 (既定のルールセット) |

**1回だけ測り、やり直していません。**

Mantine (第3回) も同じ日・同じ機械で測っています。

**記載を1件訂正しました (2026-08-02)。** 「土台」の TypeScript を当初 7.0.2 と書いていましたが、
実際に 7.0.2 なのは baseline だけで、ライブラリを使う3実装 (shadcn/ui・MUI・Mantine) は 6.0.3 です。
`tsc` は型検査のみでバンドル出力を作らないため、**掲載している測定値は変わりません**。
土台を完全に揃えるという前提に対する記載漏れなので、数字ではなく記載のほうを直しました。

---

## 一覧

| | baseline (ライブラリ無し) | shadcn/ui | MUI (Community) | Mantine |
|---|---|---|---|---|
| JS 転送量 (gzip) | 64.12 kB | 139.84 kB | **211.49 kB** | 143.04 kB |
| CSS 転送量 (gzip) | 1.17 kB | 10.77 kB | **0 (CSS-in-JS で JS に含む)** | **37.12 kB** |
| フォント | 0 | woff2 5本 (計 76.4 kB) | 0 (Roboto は読み込んでいない) | 0 |
| **自作行数** | 610 | 340 | 307 | **269** |
| **取り込み行数** | 0 | **1,472** | 0 | 0 |
| 依存パッケージ (dep + dev) | 2 + 6 | 17 + 7 | 8 + 7 | 7 + 7 |
| 共有ロジック (全実装同一) | 120 行 | 120 行 | 120 行 | 120 行 |

自作行数 = 各 kit の `src/` のうち自分で書いたもの。
取り込み行数 = CLI が生成して自分のリポジトリに置かれた部品のソース。

**方式の違いがここに出ています。** shadcn/ui は部品のソースを自分のリポジトリにコピーするので
取り込み 1,472 行を抱えます。MUI は npm から入れる普通のライブラリなので取り込みは 0 ですが、
そのぶん転送量が最大になります。**「書く量」「抱える量」「送る量」は別々の指標です。**

**Mantine は書く量が最少 (269 行) でした。** ただし CSS 転送量は 37.12 kB で4実装中の最大です。
Mantine 7 以降はスタイルを CSS-in-JS ではなく素の CSS で持つ設計なので、**JS ではなく CSS 側に出ます**。
JS と CSS を足すと Mantine 180.16 kB / MUI 211.49 kB / shadcn/ui 150.61 kB (別途フォント 76.4 kB) で、
**「どちらのファイルに乗るか」が違うだけの部分がある**ため、JS だけを並べて比べると判断を誤ります。

---

## 軸5 アクセシビリティ

| | baseline | shadcn/ui | MUI | Mantine |
|---|---|---|---|---|
| axe 違反 初期表示 | 2 | 2 | 2 | **3** |
| axe 違反 ダイアログ表示中 | 0 | 0 | 0 | **2** |
| axe 違反 フォーム検証エラー中 | 2 | 2 | 2 | **3** |
| 違反の内訳 | `landmark-one-main`, `region` | 同じ | 同じ | 左記 + **`color-contrast`** |
| Tab で主要5操作に到達 | 完遂 | 完遂 | 完遂 | 完遂 |
| **↓ と Enter でコンボボックスを選べる** | **できない** | できる | できる | できる |
| **← → でタブが切り替わる** | **切り替わらない** | 切り替わる | 切り替わる | 切り替わる |
| ダイアログの焦点閉じ込め | はい | はい | はい | はい |
| Esc でダイアログが閉じる | はい | はい | はい | はい |

**baseline・shadcn/ui・MUI の3実装は自動検査が同じ結果**でした。違反2件は内容まで同一で、
「ページに `<main>` が無い」というこちらのマークアップの問題です。**UI ライブラリを入れても直りません。**

**Mantine だけ1件多く出ました (第3回の追記)。** 増えた分は `color-contrast` で、22 箇所すべてが
Mantine の**既定の淡色トークン `dimmed`** です。白背景に `#868e96` で **3.32:1**、
WCAG AA の 4.5:1 に届きません。他の3実装で同じ用途に使った淡色 (MUI の `text.secondary` など) は
基準を満たしていました。**こちらが色を触った結果ではなく、既定値のままで出ます。**
説明文・補足行の色なので、既定のまま使うと本文以外の文字が基準を外れます。

**ダイアログ表示中に 0 にならないのも Mantine だけ**です。他の3実装は背後が走査対象から外れて 0 に
なりますが、Mantine のモーダルでは背後の要素が残るため `color-contrast` と `region` が引き続き出ます。

**なお、閉じるボタンの `aria-label` は公式ドキュメントの指示どおり設定して測っています。**
Mantine の Modal と Notification は閉じるボタンを既定で出しますが、名前は自動では付きません。
公式が `closeButtonProps` で設定するよう明記しているため、**「公式ドキュメントどおりに使う」という
事前登録の条件に従い設定した状態を正式な計測値**としています (設定前は `button-name` が追加で出ました)。

**差が出るのは動かしてみないと分からない部分だけ**です。矢印キー操作は、
ライブラリを使った2つでは効き、手で組んだ baseline では効きませんでした。
**ライブラリ同士の差はここには出ません** — shadcn/ui も MUI も同じように正しく動きます。

重要度は用途で変わります。個人開発や社内ツールなら影響は小さく、
公共・教育・医療・大企業向けでは採否に直結します。

### v1 で一度測り、やり直した経緯 (隠さず記録)

v1 の共通デモは**素の HTML 要素だけで組める部品**しか含んでいませんでした。
入力欄・ボタン・表・`<dialog>` はブラウザが最初から挙動を持つため、どのライブラリでも差が出ません。
実際 v1 では axe の違反数もキーボード到達性も完全に一致しました。

そこで PREREG を v2 に上げ、コンボボックス・タブ・カレンダーを共通デモに足して測り直しました。
**まだ1本も公開していない段階での変更**で、方針は**要件を変える前に**変更履歴に記録しています。

**計測のバグも1件直しました。** 焦点の閉じ込め判定が「Tab を1回押してダイアログの内側にいるか」
という粗い実装で、押せる要素が1つしかないダイアログでは `activeElement` が `body` に見えて
誤判定していました。複数回押す判定に直した結果、全実装とも「閉じ込められている」が正しい値です。

**ダイアログ表示中だけ違反 0** になるのは、モーダル表示中に背後が `inert` 扱いになり、
初期表示にあった2件が走査対象から外れるためです。「ダイアログを出すと良くなる」ではありません。

---

## 軸2 開発速度

| | baseline | shadcn/ui | MUI | Mantine |
|---|---|---|---|---|
| 自作行数 | 610 | 340 | 307 | **269** |
| 取り込み行数 | 0 | 1,472 | 0 | 0 |
| 実質の総量 (自作 + 取り込み) | 610 | 1,812 | 307 | **269** |

**書く量が最少なのは Mantine (269 行)** で、次が MUI (307 行) です。どちらも npm から入れて
import するだけなので、自分のリポジトリには何も増えません。shadcn/ui は書く量では近い値ですが、
取り込み 1,472 行を抱えます。

Mantine が MUI より 38 行少ないのは、**トーストとダイアログを開くのに部品を組む必要が無い**
ためです。MUI は `Snackbar` の中に `Alert` を入れ、開閉の状態も自分で持ちます。
Mantine は `notifications.show()` の1行で、状態を持ちません。
逆に**並べ替え付きの表見出しは Mantine に無い**ので、そこは自分でボタンを書いています。

v1 (6要件) から v2 (9要件) で shadcn の取り込み行数が 708 → 1,472 行に増えたのは、
コンボボックスとカレンダーが重いためです (`calendar.tsx` だけで 8.1 kB)。
**部品を1つ足すごとに、抱えるコードが目に見えて増えます。**

### 手数 (スキャフォールドから完成まで)

shadcn/ui は6手。

1. `npm create vite@latest shadcn-ui -- --template react-ts`
2. `npm i tailwindcss @tailwindcss/vite`
3. `src/index.css` を `@import "tailwindcss";` の1行に
4. `@` エイリアスを `vite.config.ts` と `tsconfig` に追加
5. `npx shadcn@latest init -y -b radix -p nova`
6. `npx shadcn@latest add button input label table dialog sonner tabs popover command calendar`

MUI は2手。

1. `npm create vite@latest mui -- --template react-ts`
2. `npm i @mui/material @emotion/react @emotion/styled @mui/icons-material @mui/x-date-pickers dayjs`

Mantine も2手。

1. `npm create vite@latest mantine -- --template react-ts`
2. `npm i @mantine/core @mantine/hooks @mantine/dates @mantine/notifications dayjs`

**実際にはスキャフォールドせず、第2回 (MUI) の設定ファイルを複製しました。** 土台の版を
完全に一致させるためです (React・Vite・TypeScript・プラグインの版を揃える)。上の2手は
公式手順どおりに始めた場合の手数で、**やっていない手順を実行したことにはしていません**。

**公式の Vite 手順にある `postcss-preset-mantine` は入れていません。** これは自分で CSS を書いて
Mantine のミックスインを使う場合に要るもので、今回は独自の CSS を1行も書いていないため
不要でした。入れなくてもビルドは通り、共通デモは9要件すべて動きます。

**shadcn の CLI は書式が変わっています** (4.16.1 時点)。`-b` は色ではなく**部品ライブラリの選択**
(`base` / `radix` / `aria`)、配色テーマは `-p`。色名を `-b` に渡す古い手順は通りません。

**MUI 7 以降はシステムプロパティが外れています。** `<Stack justifyContent>` や
`<Typography fontWeight>` は型エラーになり、`sx` に移す必要があります。
古い記事のコードはそのままではビルドが通りません。

---

## 軸3 自由度 (改造3課題)

改造は別途行い、**測定対象の実装には残していません** (改造後に既定値へ戻し、撮り直しています)。

| 課題 | baseline | shadcn/ui | MUI | Mantine |
|---|---|---|---|---|
| (a) ブランド色1つで全体を差し替え | 可能 / 2箇所 | 可能 / 2箇所 (`--primary`) | 可能 / **1箇所** (`createTheme`) | 可能 / **1箇所** (`createTheme`)。ただし色は10段階を渡す |
| (b) 全部品の角丸を 0 に | 該当なし (既定で角丸なし) | 可能 / 1箇所 (`--radius`) | 可能 / **(a) と同じ1箇所** (`shape.borderRadius`) | 可能 / **(a) と同じ1箇所** (`defaultRadius`) |
| (c) ダイアログの開閉アニメを差し替え | 新規に書く必要あり | 可能 / 2箇所 (`dialog.tsx` を直接編集) | 可能 (`TransitionComponent` が公式の口) | 可能 / **(a) と同じ1箇所** (`components.Modal.defaultProps.transitionProps`) |

**MUI は (a) と (b) が同じ `createTheme` の呼び出し1箇所で済みました。** テーマが1つの入口に
まとまっている設計です。shadcn/ui は CSS 変数を書き換える方式で、明暗2つぶん触ります。

(c) の差し替え方が違います。shadcn/ui は**部品のソースが手元にある**ので `dialog.tsx` を直接開いて
書き換えます。MUI は**公式が用意した差し替え口** (`TransitionComponent`) を使います。
前者は何でもできる代わりに本家の修正が自動で来ません。後者は用意された範囲でだけ変えられます。

(b) が baseline で「該当なし」なのは、素の HTML と最小限の CSS ではそもそも角丸を付けていないためです。

**Mantine は3課題すべてが `createTheme` の呼び出し1つに収まりました。** 配色・角丸に加えて
**ダイアログのアニメまで同じテーマ定義の中で指定できる**のはこの実装だけです
(`components.Modal.defaultProps` に既定の props を置く仕組みがあるため)。
確認は実行時の CSS 変数で行い、`--mantine-radius-default` が `0rem` に、
`--mantine-primary-color-filled` が指定色に変わることを見ています。

(a) には注意点があります。**Mantine の色は10段階の配列で渡す規約**なので、「ブランド色1つ」を
そのまま渡せません。1色から10段階を作る手間が要ります (公式にも生成の案内があります)。
**「1箇所で済む」ことと「1値で済む」ことは別**です。

Mantine の改造検証は `main.tsx` 上で行い、検証後に元へ戻してビルドし直しています。
ビルド出力のハッシュ・gzip 値・行数が改造前と一致することを確認済みで、
**改造版でキャプチャは撮っていません** (第1回で起きた取り違えの再発防止)。

### 改造の確認で1回やらかしています (記録)

改造したビルドのまま `capture.mts` を回してしまい、shadcn/ui の正式なキャプチャ10枚を
改造版で上書きしました。既定値へ戻して撮り直し済みです。

---

## 軸6 保守性 (転記・2026-08-02 取得)

出典: GitHub API と npm registry API。

| 項目 | shadcn/ui (`shadcn-ui/ui`) | MUI (`mui/material-ui`) | Mantine (`mantinedev/mantine`) |
|---|---|---|---|
| スター数 | 120,287 | 98,659 | 31,520 |
| 未解決 issue | 2,199 | 1,489 | **53** |
| 最終更新 | 2026-07-31 | 2026-07-31 | 2026-07-31 |
| 最新リリース | `shadcn@4.16.1` (2026-07-31) | `v9.2.0` (2026-07-03) | `9.5.0` (2026-07-27) |
| 直近90日のコミット数 | 234 | 291 | 293 |
| 週間ダウンロード数 | 7,335,793 (`shadcn` CLI) | 10,187,092 (`@mui/material`) | 2,301,982 (`@mantine/core`) |
| ライセンス | MIT | MIT (Core) | MIT (全パッケージ) |

**ダウンロード数を単純に並べられません。** shadcn/ui の数字は**CLI パッケージ**のもので、
利用プロジェクト数ではありません (部品は npm から入れない方式のため)。
MUI の数字は部品パッケージそのものです。**性質が違う数字を並べるときは必ずこの但し書きを付けます。**

参考: `@mui/x-date-pickers` 5,186,764 / `@mui/x-data-grid` 2,990,390 (週間・同日取得)

**Mantine は規模が一回り小さく、開発の勢いは同等**です。スターも週間ダウンロード数も
3社中で最少ですが、直近90日のコミット数は 293 で最多、最終更新も同じ日です。
**未解決 issue 53 件は他の2つより一桁少ない値**ですが、これは「品質が高い」とも
「利用者が少ないので報告が集まらない」とも読めます。**この数字だけで良し悪しは決められません。**
週間ダウンロード数の 2,301,982 は `@mantine/core` のもので、
shadcn/ui の数字 (CLI パッケージ) とは性質が違います。MUI の `@mui/material` とは同じ性質で比べられます。

---

## 軸7 必要な部品 (15項目・2026-08-02 取得)

出典: shadcn は registry index (`https://ui.shadcn.com/r/index.json`、62件)、
MUI は npm registry の `license` フィールドと公式の価格ページ、
Mantine は公式ドキュメントの各部品ページと npm registry の `license` フィールド。

| # | 部品 | baseline | shadcn/ui | MUI | Mantine |
|---|---|---|---|---|---|
| 1 | テーブル | 無し (自作) | 無料 | 無料 | 無料 |
| 2 | データグリッド (仮想化・列固定) | 無し | **無し** | **有料** (基本は無料・列固定などが Pro) | **無し** |
| 3 | 日付入力 | 無し (ブラウザ標準) | 無料 | 無料 (`x-date-pickers` は MIT) | 無料 (`@mantine/dates`) |
| 4 | カレンダー | 無し (ブラウザ標準) | 無料 | 無料 | 無料 |
| 5 | グラフ | 無し | 無料 | 無料 (`x-charts` は MIT) | 無料 (`@mantine/charts`) |
| 6 | コンボボックス | 無し (自作) | 無料 | 無料 (Autocomplete) | 無料 (Combobox / Select) |
| 7 | コマンドパレット | 無し | 無料 | **無し** | 無料 (`@mantine/spotlight`) |
| 8 | トースト | 無し (自作) | 無料 | 無料 (Snackbar) | 無料 (`@mantine/notifications`) |
| 9 | ダイアログ | 無し (`<dialog>`) | 無料 | 無料 | 無料 (Modal) |
| 10 | ドロワー | 無し | 無料 | 無料 | 無料 |
| 11 | タブ | 無し (自作) | 無料 | 無料 | 無料 |
| 12 | フォーム検証 | 無し (自作) | 無料 | **無し** (表示は可・検証機構は別ライブラリ) | 無料 (`@mantine/form`) |
| 13 | ファイルアップロード | 無し | 判定保留 | 無し | 無料 (`@mantine/dropzone`) |
| 14 | リッチテキスト | 無し | **無し** | 無し | 無料 (`@mantine/tiptap`) |
| 15 | ページネーション | 無し | 無料 | 無料 | 無料 |

**Mantine は15項目中14が無料、有料は0、無しは1 (データグリッド) です。**
第3回で調べた10パッケージ (`core` `hooks` `dates` `notifications` `form` `charts` `spotlight`
`tiptap` `dropzone` `carousel`) は**すべて 9.5.0 / MIT** で、有料版という区分がありません。

**MUI に無かった3つ (コマンドパレット・フォーム検証・ファイルアップロード) を Mantine は無料で持ちます。**
逆に**データグリッドは公式に無い**のが唯一の欠けです。`Table` に固定見出しはありますが、
仮想化や列固定は持ちません (よく名前の挙がる `mantine-datatable` は公式ではなく第三者製です)。
**大量データの表が要件の中心なら、ここは Mantine 単体では埋まりません。**

### 無料と有料の境界 (ここが第2回の本題)

**shadcn/ui 本体に有料版はありません。** 62部品すべて MIT です。
有料が出てくるのは第三者が売っている画面テンプレート集で、本体の機能制限ではありません。

**MUI は本体 (Core) が MIT で、有料なのは MUI X の一部の機能です。** 価格は公式ページより:

| プラン | 価格 |
|---|---|
| Community | 無料 |
| Pro | **$299 / 年 / 開発者** |
| Premium | $599 / 年 / 開発者 |
| Enterprise | $1,399 / 年 / 開発者 |

**「グラフや日付入力そのものが有料」ではありません。** 各系統に MIT の無料版があります
(npm の `license` を直接確認済み):

| 無料 (MIT) | 有料 (商用ライセンス) |
|---|---|
| `@mui/x-data-grid` | `@mui/x-data-grid-pro` |
| `@mui/x-date-pickers` | `@mui/x-date-pickers-pro` |
| `@mui/x-charts` | `@mui/x-charts-pro` |
| `@mui/x-tree-view` / `@mui/x-scheduler` | — |

Pro で増えるのは**表の高度な機能**が中心です: 列の固定・列のリサイズ・列のグループ化・
行の並べ替え・行の固定・セル選択・ヘッダフィルタ・行/セルの編集・Excel 書き出し・
ツリーデータ・行グループ化・集計。Premium はさらにピボットや大規模データ向けが加わります。

**今回の共通デモ9要件は、MUI でも全部無料の範囲で作れました。** 有料が要るのは
「業務用の分厚い表」を作り始めてからです。

---

## キャプチャ

`baseline/shots/` `shadcn-ui/shots/` `mui/shots/` `mantine/shots/` に10枚ずつ。
**同じスクリプト (`scripts/capture.mts`) が同じ順で操作したもの**で、番号が同じなら同じ場面です。

`01-initial` `02-search` `03-combobox` `04-filter` `05-sorted` `06-dialog`
`07-tab-form` `08-calendar` `09-form-error` `10-dark`

**カレンダーの開き方が実装ごとに違いました** (撮影スクリプトを実装非依存に直しています):

- baseline: `<input type="date">`。盤面はブラウザ (と OS) のもので、**画面にもキャプチャにも写りません**
- shadcn/ui: `id="date"` のボタンを押すと盤面がページ内に開く
- MUI: 入力欄が分割入力なので**欄を押しても開かず**、右端のカレンダーアイコンでのみ開く
- Mantine: 入力欄そのものがボタン (`DatePickerInput`)。押すと盤面がページ内に開く

**Mantine の盤面は既定で英語表記**です (`Mo Tu We` / `August 2026`)。画面の他の文言は日本語なので
ここだけ英語になります。日本語化はできますが**既定値のみルールに従い、既定のまま撮っています**。

---

## まだ測っていないもの

- 公式ドキュメントに検索があるか (軸6) — **Mantine のみ確認済み** (`Ctrl + K` の検索あり)。
  shadcn/ui と MUI は未確認のまま
- MUI の `attachment` 相当 (ファイルアップロード) の有無確認
- shadcn/ui のファイルアップロード (第1回で「判定保留」のまま)
