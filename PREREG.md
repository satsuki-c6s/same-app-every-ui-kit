# 事前登録 (PREREG)

測定を始める**前**に凍結する定義です。結果を見てから閾値や条件を動かしません。
変更するときは、変更内容と日付をこのファイルの末尾に追記し、**変更前の回は測り直しません**
(測り直さない代わりに、RESULTS.md 側で「どの版の定義で測ったか」を明記します)。

定義版: **v2 (2026-08-02 凍結)**

---

## 0. 全体の前提

- 測定機は固定。同一マシン・同一 Node・同一パッケージマネージャ
- **1回だけ測る。やり直さない**。ばらつきは幅で語る
- 章 (公開のまとまり) ごとに測る。**章をまたぐ数字は測定日が違う**ことを必ず明記する
- 土台のフレームワークとビルドツールは全実装で同一にする (版を RESULTS.md に記録)

## 1. 共通デモの要件 (増やさない・減らさない)

1. 検索欄で一覧を絞り込む
2. **コンボボックス**で言語を選ぶ (入力しながら候補を絞る)
3. **タブ**で「一覧」と「登録」を切り替える
4. テーブルで表示し、列で並べ替える
5. 行を開くとダイアログが出る
6. 登録フォームの「登録日」を**カレンダー**で選ぶ
7. フォームを送ると検証エラーが出る
8. 送信結果をトーストで知らせる
9. ダークモードを切り替える (公式の機構がある場合のみ。無い場合は「無し」と記録し、自作しない)

### なぜこの9つか (v2 で 2・3・6 を足した理由)

v1 の6要件は**素の HTML 要素だけで組める部品**しか含んでいなかった。
入力欄・ボタン・表・`<dialog>` はブラウザが最初から挙動を持っているので、
**どの UI ライブラリで作っても差が出ない**。実際 v1 で測ったところ、
axe の違反数もキーボード到達性も baseline と shadcn/ui で完全に同じだった。
これでは視聴者が選ぶ材料にならない。

足した3つは**素の HTML に無く、正しく作るには仕様どおりの実装が要る**部品:

| 部品 | 期待される挙動 | 自作した場合 |
|---|---|---|
| コンボボックス | ↓↑ で候補移動・Enter で決定・Esc で閉じる・件数を読み上げる | ほぼ実装されない |
| タブ | ←→ で切替 (Tab ではない) | Tab で切り替わる誤実装が多い |
| カレンダー | 矢印で日移動・PageUp/Down で月移動・焦点管理 | まず作れない |

見た目の差も、この3つが最も大きく出る (候補リスト・タブの下線/ピル/箱・カレンダーの盤面)。

### 入れないもの (と、その理由)

| 部品 | 入れない理由 |
|---|---|
| コマンドパレット | MUI に公式のものが無い。持たないライブラリが不当に不利になる |
| データグリッド (仮想化・列固定) | MUI は有料の X Pro。共通デモに入れると比較が壊れる |
| リッチテキスト / ファイルアップロード | 持たないライブラリが多い |

これらは**共通デモには入れないが、軸7の表 (無料/有料/無し) と特徴デモで必ず扱う**。
「共通デモで測るもの」と「有無を一覧で見せるもの」を分ける。

**選定基準** (今後この基準以外で要件を足さない):
(a) 素の HTML に無い (b) **候補ライブラリ全部が無料枠に持っている**
(c) キャプチャで見た目の差が出る (d) 実装が軽い
(e) アプリとして自然に置ける (取ってつけた部品の羅列にしない)

- データ: `shared/data.json` (20件・固定)。通信なし・API キーなし
- 文言: `shared/copy.json`。実装ごとに変えない
- 画面: 1920×1080 / DPR 1

## 2. 既定値のみルール

- 公式のスキャフォールド手順 + 公式ドキュメントどおりのコンポーネント利用に限る
- **テーマトークン・配色・角丸・影を手で調整しない** (軸3の改造課題を除く)
- 軸3の改造は共通デモとは**別のディレクトリ**で行い、計測対象の実装には混ぜない

## 3. 各軸の測り方

### 軸1 デザイン — 見せる (採点しない)

- 共通デモの完成画面を同一構図で撮り、並べる。**点数・順位をつけない**
- 補助として、公式が配布するテーマ数・ブロック数を転記する (取得日必須)

### 軸2 開発速度 — 実測

| 値 | 定義 |
|---|---|
| 実装行数 | 共通デモを満たすために書いた**自分のソース**の行数 (空行・コメント含む、全実装で同一手段で数える) |
| 取り込み行数 | shadcn/ui のようにコンポーネントのソースを自リポジトリへコピーする方式の場合の、その取り込み分の行数 |
| 部品数 | 自分で書いたコンポーネントの数 |
| 手数 | スキャフォールドから共通デモ完成までの「実行したコマンド数 + 編集したファイル数」 |

- **所要時間は使わない**。自分の習熟度が交絡するため
- **shadcn/ui 問題**: 取り込み方式のライブラリは「実装行数」に取り込み分を含めるか否かで
  結果が反転する。**含めない値を主、取り込み行数を併記**とする。両方を必ず出す
- 依存パッケージ数は取り込み方式では意味が変わるため、**軸には入れない** (参考として記録のみ)

### 軸3 自由度 — 実測 (全回同一の改造3課題)

| # | 課題 | 判定 |
|---|---|---|
| a | ブランド色を1つ指定して全体の配色を差し替える | 公式手順で可能 / 可能だが手作業が要る / 不可 |
| b | 全部品の角丸を 0 にする | 同上 |
| c | ダイアログの開閉アニメーションを差し替える | 同上 |

- **公式ドキュメントに載っている手段だけを使う**。CSS の上書きで力技で通さない
  (力技はどのライブラリでも通るので差が出ない)
- 各課題の「編集した箇所数」も記録する

### 軸4 技術構成 — 転記

- 公式ドキュメント記載の対応フレームワーク (React / Vue / Svelte / Angular / 素の HTML)
- SSR 対応、React Server Components 対応の有無
- スタイルの仕組み (Tailwind / CSS-in-JS / プレーン CSS)
- **取得日を必ず記録する**

### 軸5 アクセシビリティ — 実測

- axe による自動検査の違反数。**ルールセットのバージョンを固定して記録**
- 対象画面: 共通デモの初期表示 / ダイアログを開いた状態 / フォーム検証エラー表示中 の3状態
- キーボードだけで共通デモ6要件を完遂できるか: 完遂 / 一部 / 不可
- ダイアログの焦点閉じ込め、Esc で閉じるかを個別に記録

### 軸6 保守性 — 転記 (すべて取得日必須)

- 直近90日のコミット数
- 最新リリースの日付とバージョン
- 未解決 issue 数
- 週間ダウンロード数
- 公式ドキュメントに検索機能があるか / 日本語訳の有無

### 軸7 必要な部品 — 実測 (15項目・全回同一)

公式ドキュメントを見て **「無料 / 有料 / 無し」の3値**で判定する。

1. テーブル
2. データグリッド (仮想化・列固定)
3. 日付入力
4. カレンダー
5. グラフ
6. コンボボックス
7. コマンドパレット
8. トースト
9. ダイアログ
10. ドロワー
11. タブ
12. フォーム検証
13. ファイルアップロード
14. リッチテキスト
15. ページネーション

- 「無料だが実験的・不安定」と明記されているものは**無料**に数え、備考に書く
- 有料判定の根拠 URL と確認日を記録する

## 4. キャプチャ

`<kit>/shots/` に PNG。ファイル名は全ライブラリ共通の連番。

| ファイル | 場面 |
|---|---|
| `01-initial.png` | 初期表示 |
| `02-search.png` | 検索で絞り込んだ状態 |
| `03-combobox.png` | **コンボボックスを開いて候補が出ている状態** |
| `04-filter.png` | 言語を選んで絞り込んだ状態 |
| `05-sorted.png` | 列で並べ替えた状態 |
| `06-dialog.png` | ダイアログを開いた状態 |
| `07-tab-form.png` | **登録タブに切り替えた状態** |
| `08-calendar.png` | **カレンダーを開いた状態** |
| `09-form-error.png` | フォーム検証エラー + トースト |
| `10-dark.png` | ダークモード (機構がある場合のみ) |

`03` `07` `08` が v2 で足した「差が出る部品」の絵。
とくに `08-calendar.png` は、盤面を見ればどのライブラリか判別できるほど差が出る。

- `scripts/capture.mts` の出力のみ。手撮り禁止
- 1920×1080 / DPR 1 / 同一ブラウザ・同一手順
- **PNG のメタデータを落としてからコミットする** (撮影環境の文字列が残るため)
- 動画・トレースはコミットしない

## 5. 記録の形式

各ライブラリのディレクトリに `result.json` を置き、`RESULTS.md` はそこから起こす。
以下は形だけの雛形 (値はプレースホルダ)。

```json
{
  "kit": "shadcn-ui",
  "version": "0.0.0",
  "measuredAt": "2026-08-01",
  "prereg": "v1",
  "base": { "framework": "react@0.0.0", "bundler": "vite@0.0.0", "node": "0.0.0" },
  "axes": {
    "speed": { "implLines": 0, "vendoredLines": 0, "components": 0, "steps": 0 },
    "freedom": { "a": "possible", "b": "manual", "c": "impossible", "editedPlaces": 0 },
    "stack": { "frameworks": [], "ssr": true, "rsc": false, "styling": "", "fetchedAt": "2026-08-01" },
    "a11y": { "axeVersion": "", "violations": { "initial": 0, "dialog": 0, "formError": 0 },
              "keyboard": "complete", "focusTrap": true, "escClose": true },
    "maintenance": { "commits90d": 0, "latestRelease": "2026-08-01", "openIssues": 0,
                     "weeklyDownloads": 0, "docsSearch": true, "fetchedAt": "2026-08-01" }
  },
  "parts": [{ "name": "テーブル", "tier": "free", "source": "https://…", "fetchedAt": "2026-08-01" }],
  "aux": { "jsGzipBytes": 0, "cssGzipBytes": 0, "deps": 0 },
  "deepDive": ["design", "parts", "a11y"]
}
```

- `deepDive` は動画で深掘りした3軸。**7軸すべてを深く語ると尺に収まらない**ため、
  回ごとに3軸を選び、どれを選んだかを残す

---

## 変更履歴

- 2026-08-01: v1 凍結 (初版)
- 2026-08-02: **v2 凍結**。共通デモを6要件から9要件へ。コンボボックス・タブ・カレンダーを追加し、
  言語フィルタをボタン並びからコンボボックスへ変更。キャプチャを7枚から10枚へ。

  **理由**: v1 は素の HTML 要素だけで組める部品しか含まず、baseline と shadcn/ui で
  アクセシビリティの測定値が完全に一致した。差が出ない課題では視聴者が選ぶ材料にならない。

  **v1 で測った値の扱い**: v1 の測定は baseline と shadcn/ui の2実装のみで、**まだ1本も公開していない**。
  よって v1 の数字は破棄し、v2 で測り直す (公開済みの回が無いため、測り直しても
  「後から条件を変えて有利な数字にした」ことにはならない)。
  この判断は**要件を変える前に**記録した。以後、公開済みの回が出た後は同じことをしない。
